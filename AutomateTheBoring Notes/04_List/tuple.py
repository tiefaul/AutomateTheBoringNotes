name = ('Tyler', 'Sigird', 'Jaden') # This is a type of list, however it cannot be appended or removed (its a tuple)
print(name[0])

convert_tuple = list(name) # Converts tuple (name) into a list
print(type(convert_tuple))
print(convert_tuple)